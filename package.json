{
  "name": "obsidian-theme-animal-island",
  "version": "1.0.3",
  "scripts": {
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "lint": "stylelint \"**/*.css\"",
    "lint-fix": "stylelint \"**/*.css\" --fix"
  },
  "devDependencies": {
    "stylelint": "^17.15.0",
    "stylelint-config-obsidianmd": "^0.1.0"
  }
}
